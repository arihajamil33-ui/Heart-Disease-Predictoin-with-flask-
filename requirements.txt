# Heart Disease Prediction - Python dependencies
# Install everything with:  pip install -r requirements.txt
#
# These are the exact versions the project was developed and tested with.
# scikit-learn is pinned because the saved .pkl model files were created
# with this version and a different one may refuse to load them.

# --- Web framework ---
Flask==3.1.2

# --- Production server ---
waitress==3.0.2

# --- Data handling ---
pandas==2.3.3
numpy==2.4.2

# --- Machine learning ---
scikit-learn==1.9.0
xgboost==3.4.0
